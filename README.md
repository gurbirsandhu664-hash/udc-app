# UDC Search Engine V28
No 2700-title local key. Gemini is the only AI final classifier. Groq is research-only. Gemini can use Google Search grounding. API failures are handled without exposing raw provider errors. Never invents 0/random UDC.
Render: npm install / npm start.
Keep API keys in Environment Variables, never GitHub.
