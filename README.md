# UDC One-Click V45 ULTRA

Random-title hardened stable UDC classifier. Uses UDC only, not DDC.

Start with `npm start`.

Important: deploy the complete bundle together; do not mix files from older releases.
